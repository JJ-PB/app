<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    >
    <uses-feature
        android:name="android.hardware.camera.flash"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">
        <activity
            android:name=".AlimentosActivity"
            android:exported="true" />
        <activity
            android:name=".ReclamosActivity"
            android:exported="true" />
        <activity
            android:name=".PerfilActivity"
            android:exported="true" />
        <activity
            android:name=".CinesActivity"
            android:exported="true" />
        <activity
            android:name=".CinehoytsActivity"
            android:exported="true" />
        <activity
            android:name=".CinemarkActivity"
            android:exported="true" />
        <activity
            android:name=".CinepolisActivity"
            android:exported="true" />
        <activity
            android:name=".NormandieActivity"
            android:exported="true" />
        <activity
            android:name=".HomeActivity"
            android:exported="true" />
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Permisos para acceder a archivos -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/ruta_archivo" />
        </provider>
    </application>

</manifest>